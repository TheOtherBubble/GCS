// Remove explicit `width` and `height` and use a `viewBox` instead so that SVGs can scale.
const config = { dimensions: false };

/**
 * The configuration for SVGR.
 * @see {@link https://react-svgr.com/docs/configuration-files/ | Configuration Files}
 * @see {@link https://react-svgr.com/docs/options/ | Options}
 * @internal
 */
export default config;

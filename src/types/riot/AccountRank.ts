/**
 * Ranks that an account can be within a tier.
 * @public
 */
enum AccountRank {
	/** One (highest). */
	I = "I",

	/** Two (second-highest). */
	II = "II",

	/** Three (second-lowest). */
	III = "III",

	/** Four (lowest). */
	IV = "IV"
}

export default AccountRank;

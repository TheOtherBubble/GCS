/**
 * A document.
 * @public
 */
enum Document {
	/** The rulebook. */
	RULEBOOK = 0,

	/** The sign-up questions. */
	SIGN_UP_QUESTIONS = 1,

	/** The landing page blurb. */
	LANDING_BLURB = 2
}

export default Document;
